# ============================================================================
# CleanUNet2-wav2vec2: Requirements
# ============================================================================
# This project supports TWO types of self-supervised speech embeddings:
#   1. X-Vectors (SpeechBrain) - 512 dimensions
#   2. Wav2Vec2 (facebook/wav2vec2-xls-r-300m) - 1024 dimensions [NEW!]
#
# To use Wav2Vec2 embeddings (recommended):
#   - See WAV2VEC2_GUIDE.md for complete setup instructions
#   - Run: pip install -r requirements.txt
#   - Run: python extract_wav2vec2_embeddings.py --config configs/train_wav2vec2_stage1.yaml
#   - Train: python train_xvector.py --config configs/train_wav2vec2_stage1.yaml --stage stage1
# ============================================================================

# --- Core Deep Learning ---
torch>=2.1.0
torchaudio>=2.1.0
pytorch-lightning>=2.2.0

# --- Audio Processing ---
librosa>=0.10.1
soundfile>=0.12.1
tqdm>=4.65.0

# --- Metrics (PESQ/STOI) ---
pesq>=0.0.4
pystoi>=0.3.3
torchmetrics[audio]

# --- Numeric / Utils ---
numpy>=1.23.0
scipy>=1.10.0
PyYAML>=6.0
json5>=0.9.14

# --- Logging & Experimentation ---
tensorboard>=2.14.0
wandb

# --- Optional but Recommended ---
matplotlib>=3.6.0
einops>=0.7.0

# --- X-Vector Integration (Two-Stage Training) ---
speechbrain>=0.5.16  # For X-Vector extraction
ruamel.yaml  # Additional YAML support for SpeechBrain

# --- Wav2Vec2 Integration (Two-Stage Training - NEW!) ---
transformers>=4.30.0  # For Wav2Vec2 models (facebook/wav2vec2-xls-r-300m)
huggingface_hub>=0.20.0  # For downloading pretrained models from HuggingFace
safetensors>=0.4.0  # Secure model loading (required by transformers)
sentencepiece>=0.1.99  # Required by some transformers tokenizers (optional)

# --- Data Augmentation ---
torch-audiomentations>=0.11.0  # On-the-fly audio augmentation

