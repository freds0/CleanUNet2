# ============================================================================
# CleanUNet2-wav2vec2: Requirements (Wav2Vec2 ONLY - XVector removed)
# ============================================================================
# This project uses ONLY Wav2Vec2 embeddings (facebook/wav2vec2-xls-r-2b):
#   - 1024-dimensional embeddings from layer 24
#   - Extracted via HuggingFace transformers
#   - Supports both pre-extracted caching and on-the-fly extraction
#
# Installation:
#   pip install -r requirements.txt
#
# Quick test:
#   python scripts/train.py --config configs/train.yaml --quick-test
# ============================================================================

# --- Core Deep Learning (PyTorch Stack) ---
torch>=2.1.0
torchaudio>=2.1.0
pytorch-lightning>=2.2.0

# --- Audio Processing ---
librosa>=0.10.1
soundfile>=0.12.1
tqdm>=4.65.0

# --- Metrics (PESQ/STOI/SI-SDR) ---
pesq>=0.0.4
pystoi>=0.3.3
torchmetrics[audio]

# --- Numeric & Data Utilities ---
numpy>=1.23.0
scipy>=1.10.0
PyYAML>=6.0

# --- Logging & Experimentation ---
tensorboard>=2.14.0
wandb

# --- Visualization (Optional) ---
matplotlib>=3.6.0

# --- Tensor Operations & Model Utilities ---
einops>=0.7.0

# --- Wav2Vec2 Embeddings (Required) ---
transformers>=4.30.0          # Wav2Vec2 models from HuggingFace Hub
huggingface_hub>=0.20.0       # Download pretrained models
safetensors>=0.4.0            # Secure model loading

# --- Data Augmentation ---
torch-audiomentations>=0.11.0  # On-the-fly audio augmentation

# --- Optional but Recommended ---
# sentencepiece>=0.1.99        # For some tokenizer requirements (optional)

